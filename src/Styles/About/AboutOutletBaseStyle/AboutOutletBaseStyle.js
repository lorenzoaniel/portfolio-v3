import { css } from "styled-components";

export const AboutOutletBaseStyle = css`
	background-color: var(--darkgrey-outletinner-bg);
	height: 95%;
	width: 95%;
	border-radius: 0.5rem;
	box-shadow: var(--black-shadow-inset), var(--black-shadow-outset);
	padding: 0.7vw;
`;
